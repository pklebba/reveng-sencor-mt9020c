# Copy this file to `secrets.yaml` and fill in your values.
# `secrets.yaml` is gitignored so credentials never get committed.
wifi_ssid: "YOUR_WIFI"
wifi_password: "YOUR_PASSWORD"
api_key: "BASE64_32_BYTE_KEY"     # openssl rand -base64 32
ota_password: "SOME_OTA_PASSWORD"
